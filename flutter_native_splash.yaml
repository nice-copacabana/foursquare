# Generated by Qoder AI (Model: claude-sonnet-4-5-20250929) - 2025-10-22
# Task: 配置Flutter应用启动屏幕

# flutter_native_splash配置
# 运行命令: flutter pub run flutter_native_splash:create

flutter_native_splash:
  # 背景色（主题蓝色）
  color: "#1976D2"
  color_dark: "#1565C0"  # 深色模式背景色
  
  # Logo图片
  image: assets/splash/logo.png  # 512x512 Logo
  image_dark: assets/splash/logo_dark.png  # 深色模式Logo（可选）
  
  # Android配置
  android: true
  android_12:
    # Android 12+使用新的Splash Screen API
    image: assets/splash/logo.png
    icon_background_color: "#1976D2"
    image_dark: assets/splash/logo_dark.png
    icon_background_color_dark: "#1565C0"
  
  android_gravity: center  # 图片居中
  
  # iOS配置
  ios: true
  ios_content_mode: center
  
  # Web配置
  web: true
  web_image_mode: center
  
  # 全屏模式（false显示状态栏）
  fullscreen: false
  
  # 品牌图片（底部，可选）
  # branding: assets/splash/branding.png
  # branding_dark: assets/splash/branding_dark.png
  # branding_mode: bottom
  
  # 信息图片（底部文字，可选）
  # info_plist_files:
  #   - 'ios/Runner/Info.plist'
