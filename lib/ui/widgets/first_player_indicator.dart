// Generated by Qoder AI (Model: gemini-3-pro) - 2025-11-25
// Task: 创建先手方提示动画组件

import 'package:flutter/material.dart';
import '../../models/piece_type.dart';

/// 先手方指示器组件
/// 
/// 在双人对战开始时显示先手方信息的动画提示
class FirstPlayerIndicator extends StatefulWidget {
  /// 先手方（黑方或白方）
  final PieceType firstPlayer;
  
  /// 动画完成回调
  final VoidCallback? onAnimationComplete;
  
  /// 动画持续时间（毫秒）
  final int duration;

  const FirstPlayerIndicator({
    super.key,
    required this.firstPlayer,
    this.onAnimationComplete,
    this.duration = 2500,
  });

  @override
  State<FirstPlayerIndicator> createState() => _FirstPlayerIndicatorState();
}

class _FirstPlayerIndicatorState extends State<FirstPlayerIndicator>
    with SingleTickerProviderStateMixin {
  late AnimationController _controller;
  late Animation<double> _scaleAnimation;
  late Animation<double> _opacityAnimation;

  @override
  void initState() {
    super.initState();
    
    _controller = AnimationController(
      duration: Duration(milliseconds: widget.duration),
      vsync: this,
    );

    // 缩放动画：0.5 -> 1.2 -> 1.0 -> 0.8
    _scaleAnimation = TweenSequence<double>([
      TweenSequenceItem(
        tween: Tween<double>(begin: 0.5, end: 1.2)
            .chain(CurveTween(curve: Curves.easeOutBack)),
        weight: 30.0,
      ),
      TweenSequenceItem(
        tween: Tween<double>(begin: 1.2, end: 1.0)
            .chain(CurveTween(curve: Curves.easeInOut)),
        weight: 20.0,
      ),
      TweenSequenceItem(
        tween: ConstantTween<double>(1.0),
        weight: 30.0,
      ),
      TweenSequenceItem(
        tween: Tween<double>(begin: 1.0, end: 0.8)
            .chain(CurveTween(curve: Curves.easeIn)),
        weight: 20.0,
      ),
    ]).animate(_controller);

    // 不透明度动画：0.0 -> 1.0 -> 1.0 -> 0.0
    _opacityAnimation = TweenSequence<double>([
      TweenSequenceItem(
        tween: Tween<double>(begin: 0.0, end: 1.0)
            .chain(CurveTween(curve: Curves.easeIn)),
        weight: 20.0,
      ),
      TweenSequenceItem(
        tween: ConstantTween<double>(1.0),
        weight: 60.0,
      ),
      TweenSequenceItem(
        tween: Tween<double>(begin: 1.0, end: 0.0)
            .chain(CurveTween(curve: Curves.easeOut)),
        weight: 20.0,
      ),
    ]).animate(_controller);

    _controller.forward().then((_) {
      if (widget.onAnimationComplete != null) {
        widget.onAnimationComplete!();
      }
    });
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final isBlack = widget.firstPlayer == PieceType.black;
    final playerName = isBlack ? '黑方' : '白方';
    final playerColor = isBlack ? Colors.black : Colors.white;
    final backgroundColor = isBlack ? Colors.white : Colors.black87;

    return AnimatedBuilder(
      animation: _controller,
      builder: (context, child) {
        return Opacity(
          opacity: _opacityAnimation.value,
          child: Transform.scale(
            scale: _scaleAnimation.value,
            child: Container(
              padding: const EdgeInsets.symmetric(
                horizontal: 48,
                vertical: 24,
              ),
              decoration: BoxDecoration(
                color: backgroundColor,
                borderRadius: BorderRadius.circular(16),
                boxShadow: [
                  BoxShadow(
                    color: playerColor.withOpacity(0.3),
                    blurRadius: 20,
                    spreadRadius: 5,
                  ),
                ],
                border: Border.all(
                  color: playerColor.withOpacity(0.5),
                  width: 2,
                ),
              ),
              child: Column(
                mainAxisSize: MainAxisSize.min,
                children: [
                  // 棋子图标
                  Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                      shape: BoxShape.circle,
                      color: playerColor,
                      border: Border.all(
                        color: backgroundColor,
                        width: 3,
                      ),
                      boxShadow: [
                        BoxShadow(
                          color: playerColor.withOpacity(0.5),
                          blurRadius: 10,
                          spreadRadius: 2,
                        ),
                      ],
                    ),
                  ),
                  const SizedBox(height: 16),
                  // 先手文本
                  Text(
                    '$playerName先手',
                    style: TextStyle(
                      fontSize: 28,
                      fontWeight: FontWeight.bold,
                      color: playerColor,
                      letterSpacing: 2,
                    ),
                  ),
                  const SizedBox(height: 8),
                  // 提示文本
                  Text(
                    '游戏即将开始',
                    style: TextStyle(
                      fontSize: 16,
                      color: playerColor.withOpacity(0.7),
                      letterSpacing: 1,
                    ),
                  ),
                ],
              ),
            ),
          ),
        );
      },
    );
  }
}
