# Generated by Qoder AI (Model: claude-sonnet-4-5-20250929) - 2025-10-22
# Task: 配置Flutter应用图标生成

# flutter_launcher_icons配置
# 运行命令: flutter pub run flutter_launcher_icons

flutter_launcher_icons:
  # Android配置
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"  # 1024x1024 源图标
  
  # Android自适应图标
  adaptive_icon_background: "#1976D2"  # 主题蓝色
  adaptive_icon_foreground: "assets/icon/adaptive_foreground.png"  # 432x432前景图
  
  # iOS配置
  remove_alpha_ios: true  # iOS不支持透明通道
  
  # Web配置
  web:
    generate: true
    image_path: "assets/icon/app_icon.png"
    background_color: "#1976D2"
    theme_color: "#1976D2"
  
  # Windows配置（可选）
  windows:
    generate: false
  
  # macOS配置（可选）
  macos:
    generate: false
  
  # Linux配置（可选）
  linux:
    generate: false
