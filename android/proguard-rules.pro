# Generated by Qoder AI (Model: claude-sonnet-4-5-20250929) - 2025-10-22
# Task: Android ProGuard混淆规则配置

# Flutter相关保留规则
-keep class io.flutter.app.** { *; }
-keep class io.flutter.plugin.**  { *; }
-keep class io.flutter.util.**  { *; }
-keep class io.flutter.view.**  { *; }
-keep class io.flutter.**  { *; }
-keep class io.flutter.plugins.**  { *; }

# Dart相关保留
-keep class androidx.lifecycle.DefaultLifecycleObserver

# Hive数据库相关
-keep class * extends com.hivedb.** { *; }
-keep class hive.** { *; }
-keepnames class * extends hive.**
-keepclassmembers class * extends hive.** {
    <fields>;
}

# 数据模型类保留（用于序列化）
-keep class com.qoder.foursquare.models.** { *; }
-keepclassmembers class com.qoder.foursquare.models.** {
    <fields>;
    <methods>;
}

# WebSocket相关
-keep class io.flutter.plugins.webviewflutter.** { *; }
-dontwarn io.flutter.embedding.**

# AudioPlayers相关
-keep class xyz.luan.audioplayers.** { *; }

# SharedPreferences相关
-keep class androidx.preference.** { *; }

# 保留泛型签名
-keepattributes Signature
-keepattributes *Annotation*
-keepattributes InnerClasses
-keepattributes EnclosingMethod

# 保留行号信息（用于调试崩溃）
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile

# 移除日志
-assumenosideeffects class android.util.Log {
    public static *** d(...);
    public static *** v(...);
    public static *** i(...);
}

# 优化相关
-optimizationpasses 5
-dontusemixedcaseclassnames
-dontskipnonpubliclibraryclasses
-verbose

# 保留native方法
-keepclasseswithmembernames class * {
    native <methods>;
}

# 保留自定义View
-keepclasseswithmembers class * {
    public <init>(android.content.Context, android.util.AttributeSet);
}
-keepclasseswithmembers class * {
    public <init>(android.content.Context, android.util.AttributeSet, int);
}

# 保留枚举
-keepclassmembers enum * {
    public static **[] values();
    public static ** valueOf(java.lang.String);
}

# 保留Parcelable
-keep class * implements android.os.Parcelable {
    public static final android.os.Parcelable$Creator *;
}

# 保留Serializable
-keepclassmembers class * implements java.io.Serializable {
    static final long serialVersionUID;
    private static final java.io.ObjectStreamField[] serialPersistentFields;
    private void writeObject(java.io.ObjectOutputStream);
    private void readObject(java.io.ObjectInputStream);
    java.lang.Object writeReplace();
    java.lang.Object readResolve();
}

# Gson相关（如果使用）
-keepattributes Signature
-keepattributes *Annotation*
-keep class sun.misc.Unsafe { *; }
-keep class com.google.gson.** { *; }

# 保留反射使用的类
-keep class * {
    public protected *;
}

# AndroidX相关
-keep class androidx.** { *; }
-keep interface androidx.** { *; }
-dontwarn androidx.**

# 保留R文件
-keepclassmembers class **.R$* {
    public static <fields>;
}

# 四子游戏特定规则
# 保留游戏引擎相关类
-keep class com.qoder.foursquare.engine.** { *; }
-keep class com.qoder.foursquare.ai.** { *; }
-keep class com.qoder.foursquare.bloc.** { *; }
-keep class com.qoder.foursquare.services.** { *; }

# 保留BLoC状态和事件类
-keepclassmembers class * extends io.flutter.embedding.engine.plugins.FlutterPlugin {
    <methods>;
}
