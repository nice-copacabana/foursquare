// Generated by Qoder AI (Model: claude-sonnet-4-5-20250929) - 2025-10-22
// Task: PieceType枚举单元测试

import 'package:flutter_test/flutter_test.dart';
import 'package:foursquare/models/piece_type.dart';

void main() {
  group('PieceType', () {
    group('枚举值', () {
      test('应包含所有预期的枚举值', () {
        expect(PieceType.values.length, 3);
        expect(PieceType.values, contains(PieceType.empty));
        expect(PieceType.values, contains(PieceType.black));
        expect(PieceType.values, contains(PieceType.white));
      });
    });

    group('getOpponent', () {
      test('黑方的对手应为白方', () {
        expect(PieceType.black.getOpponent(), PieceType.white);
      });

      test('白方的对手应为黑方', () {
        expect(PieceType.white.getOpponent(), PieceType.black);
      });

      test('空位的对手应为空位', () {
        expect(PieceType.empty.getOpponent(), PieceType.empty);
      });
    });

    group('isPlayer', () {
      test('黑方应为玩家棋子', () {
        expect(PieceType.black.isPlayer(), true);
      });

      test('白方应为玩家棋子', () {
        expect(PieceType.white.isPlayer(), true);
      });

      test('空位不应为玩家棋子', () {
        expect(PieceType.empty.isPlayer(), false);
      });
    });

    group('getDisplayName', () {
      test('黑方显示名称应为"黑方"', () {
        expect(PieceType.black.getDisplayName(), '黑方');
      });

      test('白方显示名称应为"白方"', () {
        expect(PieceType.white.getDisplayName(), '白方');
      });

      test('空位显示名称应为"空位"', () {
        expect(PieceType.empty.getDisplayName(), '空位');
      });
    });

    group('getSymbol', () {
      test('黑方符号应为●', () {
        expect(PieceType.black.getSymbol(), '●');
      });

      test('白方符号应为○', () {
        expect(PieceType.white.getSymbol(), '○');
      });

      test('空位符号应为·', () {
        expect(PieceType.empty.getSymbol(), '·');
      });
    });
  });
}
